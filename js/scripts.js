var PROJECTNAME = PROJECTNAME || {};

$(function() {

    PROJECTNAME = {

        init: function() {

            this.handleMenu();

            // if ( $('.content-slider').length ) {
            //     this.handleSlider();
            // }

        },

        handleMenu: function() {
            //menu
        },

    };

    PROJECTNAME.init();

});